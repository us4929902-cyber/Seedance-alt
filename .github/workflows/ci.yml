CI - Node.js

on: push: branches: [ main, seedance-scaffold ] pull_request: branches: [ main ]

jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - name: Use Node.js 18 uses: actions/setup-node@v4 with: node-version: '18' - name: Install dependencies run: npm ci - name: Run GPT test (no key required) run: node test_gpt.js || true - name: Lint (placeholder) run: echo "No lint configured"
